# Optional local overrides for the Royal Ride Vite app.
# The app defaults to these values when they are not supplied.
PORT=19432
BASE_PATH=/

# The separate API/DB workspace uses DATABASE_URL when run locally.
# Keep the real connection string in a secret or local .env file only.
# DATABASE_URL=